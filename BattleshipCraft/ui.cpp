#include "ui.h"
#include <iostream>

UI::UI() {}

void UI::renderCoordinates(int x, int y, int z) const {
    // Placeholder: Will implement with renderText once SDL_ttf is fixed
    std::cout << "Coordinates: (" << x << ", " << y << ", " << z << ")" << std::endl;
}